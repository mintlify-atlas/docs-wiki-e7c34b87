---
title: "Codes API — Apache Fineract Code Definition CRUD"
sidebarTitle: "Codes"
description: "Reference for Apache Fineract /v1/codes endpoints that define Codes — the typed categories whose entries (Code Values) drive dropdowns across the platform."
---

The Codes API manages the top-level `Code` rows in Apache Fineract. A Code defines a category (for example `Gender`, `LoanPurpose`, `IDType`, `AssetAccountTag`) and is the parent of the individual `CodeValue` rows surfaced through dropdowns across loans, clients, and accounting. System-defined codes ship with the platform and cannot be modified; user-defined codes can be renamed and deleted.

## Source

| Aspect | Value |
| --- | --- |
| Resource class | `org.apache.fineract.infrastructure.codes.api.CodesApiResource` |
| File | `fineract-provider/src/main/java/org/apache/fineract/infrastructure/codes/api/CodesApiResource.java` |
| JAX-RS `@Path` | `/v1/codes` |
| Swagger tag | `Codes` |
| Permission code | `CODE` |
| Read service | `CodeReadPlatformService` |
| Response parameters | `id`, `name`, `systemDefined` |

## Endpoints

| Method | Path | Description | Command / read handler | Permission |
| --- | --- | --- | --- | --- |
| `GET` | `/v1/codes` | List all codes. | `CodeReadPlatformService.retrieveAllCodes()` | `READ_CODE` |
| `GET` | `/v1/codes/{codeId}` | Retrieve a single code by id. | `CodeReadPlatformService.retrieveCode(codeId)` | Authenticated |
| `GET` | `/v1/codes/name/{codeName}` | Retrieve a code by name. | `CodeReadPlatformService.retrieveCode(codeName)` | Authenticated |
| `POST` | `/v1/codes` | Create a user-defined code. `systemDefined` is forced to `false`. | `CommandWrapperBuilder.createCode()` → `CREATE_CODE` | `CREATE_CODE` |
| `PUT` | `/v1/codes/{codeId}` | Update a non-system-defined code. | `updateCode(codeId)` → `UPDATE_CODE` | `UPDATE_CODE` |
| `DELETE` | `/v1/codes/{codeId}` | Delete a non-system-defined code. | `deleteCode(codeId)` → `DELETE_CODE` | `DELETE_CODE` |

## Request body — create / update

```json
{
  "name": "LoanPurpose"
}
```

The update endpoint accepts the same shape; system-defined codes are rejected at the command-handler level.

## Response — list

```json
[
  { "id": 1,  "name": "Gender",         "systemDefined": true },
  { "id": 14, "name": "LoanPurpose",    "systemDefined": false },
  { "id": 22, "name": "AssetAccountTag","systemDefined": true }
]
```

## Response — single

```json
{ "id": 14, "name": "LoanPurpose", "systemDefined": false }
```

## Response — write

```json
{
  "resourceId": 14,
  "changes": { "name": "LoanPurpose" }
}
```

## Code-value sub-resource

Codes are paired with `CodeValuesApiResource` mounted under the same `/v1/codes` path. The code-values endpoints (`/v1/codes/{codeId}/codevalues`, `/v1/codes/name/{codeName}/codevalues`, etc.) are documented in [/api/code-values](/api/code-values).

## Common system-defined codes

| Code name | Used by |
| --- | --- |
| `Gender` | Client demographics. |
| `CustomerIdentifier` | Client KYC. |
| `LoanPurpose` | Loan applications. |
| `AssetAccountTag`, `LiabilityAccountTag`, `EquityAccountTag`, `IncomeAccountTag`, `ExpensesAccountTag` | Accounting rule tagging. |
| `LoanCollateral` | Legacy collateral types (see [/api/collaterals](/api/collaterals)). |

## Related subsystems

- Subsystem overview: [/infrastructure/codes-and-code-values](/core/codes)
- Per-code values CRUD: [/api/code-values](/api/code-values)
- Accounting tags consumers: [/api/accounting-rules](/api/accounting-rules), [/api/gl-accounts](/api/gl-accounts)
